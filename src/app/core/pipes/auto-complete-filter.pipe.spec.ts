import { AutoCompleteFilterPipe } from './auto-complete-filter.pipe';

describe('AutoCompleteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoCompleteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
